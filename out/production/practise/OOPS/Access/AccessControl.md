private - accesable only inside
the same class only

default - only accessble inside the packge not outside

when to use which access modifier
private - for most sensitive data .that we dont want direct access
 we can access it via setter and getter

default  - when we dont use the var outside the package
protected- ony acces outside the class it hass to be subclass 
- only subclass can access protected members in basse clas when it is in diff package


public - when wanted to access anywehere


